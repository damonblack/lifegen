<div class="life-list-item">
<h2><%= life.character_sheet[:name] || "Name: " %></h2>
<% life.character_sheet["attributes"].each do |key, value| %>
  <span><%= "#{key}:#{value}" %></span>
<% end %>
<%= button_to "Edit", edit_life_path(life.id), method: :get %>
</div>
